# Force devDependencies to be installed even when NODE_ENV=production.
# Railway/Nixpacks sets NODE_ENV=production during builds which strips devDeps,
# preventing TypeScript from being available for `npm run build --workspace=backend`.
production=false
